# Cloudscape Labs Readme

#### cordova-plugin-advanced-http

The `cordova-plugin-advanced-http` dependency been overridden with our own private fork in plugin.xml to address build issues with xcode 26.  
There is an open PR in the upstream repo to address the issue but development on the plugin is very slow, and so we
can maintain our own fork, merging upstream changes as needed.
